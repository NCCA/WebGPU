[project]
name = "ParticleQuads"
description = "Particle Systems using Quads"
requires-python = ">=3.11"
version = "0.1.0"
dependencies = [
    "nccapy",
    "wgpu",
    "pyside6",
    "numpy",
    "qtpy",
    "cffi>=1.17.1",
]
